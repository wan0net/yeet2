# Docker Infrastructure

This directory is reserved for future Docker-oriented support files beyond the top-level `docker-compose.yml`.

The MVP uses it as a placeholder for local bootstrap and service wiring notes only.

